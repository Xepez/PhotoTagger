<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PhotoTagger.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PhotoTagger"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="PhotoTagger">

    <Grid Padding="12">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel
            Grid.Row="0"
            Orientation="Horizontal"
            Spacing="12">

            <Button
                Content="Select Folder"
                Click="SelectFolderButton_Click" />

            <TextBlock
                x:Name="FolderText"
                VerticalAlignment="Center"
                Text="No folder selected"
                TextTrimming="CharacterEllipsis" />

        </StackPanel>

        <!-- Photo count -->
        <TextBlock
            x:Name="PhotoCountText"
            Grid.Row="1"
            Margin="0,8,0,8" />

        <!-- Main area -->
        <Grid Grid.Row="2">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Thumbnail list -->
            <ListView
                x:Name="PhotoList"
                Grid.Column="0"
                SelectionMode="Single"
                HorizontalContentAlignment="Stretch"
                SelectionChanged="PhotoList_SelectionChanged">

                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsWrapGrid
                            Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>

                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel
                            Width="180"
                            Margin="8">

                            <Border
                                Width="180"
                                Height="140"
                                BorderThickness="1"
                                CornerRadius="4">

                                <Image
                                    Source="{Binding Thumbnail}"
                                    Stretch="UniformToFill" />

                            </Border>

                            <TextBlock
                                Text="{Binding FileName}"
                                HorizontalAlignment="Center"
                                TextTrimming="CharacterEllipsis"
                                Margin="4,6,4,0" />

                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>

            <!-- Large preview -->
            <Border
                Grid.Column="1"
                Margin="16,0,0,0"
                BorderThickness="1"
                Padding="12">

                <Grid>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <Image
                        x:Name="PreviewImage"
                        Grid.Row="0"
                        Stretch="Uniform" />

                    <StackPanel
                        Grid.Row="1"
                        Margin="0,12,0,0">

                        <TextBlock
                            x:Name="PreviewFileName"
                            FontSize="18"
                            FontWeight="SemiBold"
                            TextWrapping="Wrap" />

                        <TextBlock
                            x:Name="PreviewFilePath"
                            Margin="0,4,0,0"
                            Opacity="0.7"
                            TextWrapping="Wrap" />

                    </StackPanel>

                </Grid>

            </Border>

        </Grid>

    </Grid>

</Window>